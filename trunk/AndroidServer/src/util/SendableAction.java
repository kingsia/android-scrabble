package util;

/**
 * enum that takes care of all
 * actions that possibly can be
 * sent to the server
 * 
 * @author Magnus
 */
public enum SendableAction {
	LOGIN,
	LOGOUT,
	SEARCH_PLAYER,
	SIGN_UP
}